# 如何使用这套 BP 阅读流程

这是一份操作备忘，回答两个最常见的问题：**怎么换项目**、**怎么持续跟进**。

> 一句话原理：**流程和所有项目档案都存在 Git 仓库里，不依赖任何单个对话框。** 对话框只是「干活的工位」，仓库才是「档案柜」。关掉对话、过几天再来都不丢。

---

## 一、新项目怎么开始

在任意对话里上传 BP（PDF），说一句：

> 读一下这个 BP

`/read-bp` 工作流会自动接手：新建 `deals/<公司名>/` 文件夹 → 归档原始 PDF → 产出 `01-分析.md`。

**换全新项目时，建议开一个新对话/新会话**，原因：上下文干净、不会被上一个项目串味。新会话里我照样能读到 `deals/` 里所有老项目（因为都在仓库里）。

| 你想做的 | 在哪做 |
|---------|--------|
| 继续当前项目（问题清单/纪要/跟进） | 当前对话直接说 |
| 换一个全新项目 | **开新对话**，上传 BP 说「读一下这个 BP」 |
| 跨项目对比（A 家 vs B 家） | 任意对话，我读 `deals/` 下的档案即可对比 |

---

## 二、四个阶段怎么触发

每个阶段独立，按需调用，不必一次跑完：

| 阶段 | 你说什么 | 产物 |
|------|----------|------|
| ① BP 阅读 | 「读一下这个 BP」 | `01-分析.md` |
| ② 问题罗列 | 「见创始人前，列要问的问题」 | `02-问题清单.md` |
| ③ 纪要整理 | 「把和创始人聊的整理成纪要」（附聊天记录/录音转写/要点） | `03-纪要-<日期>.md` |
| ④ 持续对话 | 「这项目现在啥情况」「他们说 XX，靠谱吗」 | 更新 `04-跟踪.md` |

---

## 三、怎么持续跟进一个项目

1. **见完创始人** → 把聊天要点/录音转写发我 → 我整理成 `03-纪要-<日期>.md`，并回填 `02-问题清单.md`（已答标注结论，未答标待跟进），更新结论。
2. **有新进展**（融资、订单、数据变化）→ 告诉我 → 我更新 `04-跟踪.md`，并**对照历史交叉验证**（这次说的和 BP / 上次纪要对不对得上）。
3. **隔段时间回看** → 在任意新会话问「<某公司>现在什么情况」 → 我基于该文件夹的完整档案给当前快照。

---

## 四、档案长这样

```
deals/
  <公司简称>/
    source/              # 原始 BP（PDF）
    01-分析.md           # ① 结构化分析 + 结论
    02-问题清单.md       # ② 带 P0/P1/P2 的尽调问题
    03-纪要-<日期>.md    # ③ 每次沟通一份
    04-跟踪.md           # ④ 长期跟踪事件流
```

只要这些文件在仓库里（我每步都会 commit+push），**任何时间、任何新会话**都能接着用。

---

## 五、知识库检索（搜一下就知道谁说了什么）

仓库自带一个**零依赖、可离线**的全文检索工具，把 `deals/` 下所有分析、BP 原文提取、问题清单、纪要都建成可搜索的知识库。

```bash
python3 tools/kb.py search "触觉数据壁垒"        # 搜：哪个项目、哪页、原话怎么说
python3 tools/kb.py search "估值" --project 戴世   # 限定某个项目
python3 tools/kb.py all                          # 内容更新后：重建索引 + 刷新看板
```

- **`INDEX.md`（根目录）**：所有项目的总看板（赛道/轮次/融资额/评级/结论/进度），由 `kb.py index` 自动生成。
- **`source/原文-提取.md`**：每个项目把 BP（含图片页）里的话落成可搜索文字——这是"搜得到原话"的关键。
- **检索原理**：BM25 + 中文分词，纯 Python，无需联网或 API key（当前远程环境不放行向量模型下载，故用词法检索；`tools/kb.py` 末尾留了升级到语义向量检索的接口）。
- 你也可以**直接对我说**「哪些项目提到触觉」「对比破晓和戴世的融资」，我会帮你查。

> 改了任何分析/原文后，记得让我跑一次 `python3 tools/kb.py all` 把索引和看板刷新。

---

## 六、记住三点

1. **换项目开新会话**，干净；老项目档案不丢。
2. **每一步都落盘成文件**，对话关了也在。
3. **我站在出资方立场**：发现亮点，更要盯红旗和待验证假设——不替创始人美化。
